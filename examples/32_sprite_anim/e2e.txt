# E2E: walk animation + left/right patrol (deterministic CRC baked in)
# Preconditions: 640×360 / fixed background / deterministic draw without a fixed seed / harness virtual clock
# Example run:
#   TMPD=$(mktemp -d)
#   VP_HEADLESS=1 VP_HARNESS_SCRIPT=examples/32_sprite_anim/e2e.txt \
#     VP_HARNESS_OUT=$TMPD zig build run-example_32

step 1
snapshot fb
digest fb
expect fb crc=BA8FA900

step 59
snapshot fb
digest fb
expect fb crc=0D5C6C01

step 60
snapshot fb
digest fb
expect fb crc=D357AA75

step 60
snapshot fb
digest fb
expect fb crc=6A31348B
quit
